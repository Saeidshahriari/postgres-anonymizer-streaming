-- Use these commands only before exporting data for non-prod use.
-- They are irreversible.

-- Example: anonymize a single table
-- SELECT anon.anonymize_table('customer');

-- Or anonymize the whole database (uses your declared rules)
-- SELECT anon.anonymize_database();
